import styled from 'styled-components';
import {
  Text,
  View
} from 'react-native';

export const StyledTextLoad = styled(Text)`
  align-self: center;
`;

export const StyledContainerItems = styled(View)`
  flex-direction: row;
`;
